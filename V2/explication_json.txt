{
  "coordinate": "[48.8566, 2.3522]",    => Latitude et longitude de l'emplacement
  "timezone": "Europe/Paris",           => la time zone de l'emplacement en question,  important pour avoir l'heure exact du coucher / level soleil
  "record_every_s": 10,                 => enregistre toutes les données partagé dans un le dossier record toutes X secs
  
  // Paramètre de communication entre le logiciel pure data et le python
  // Par défaut le logiciel envoie les données à chaque changement, et renvoie toutes les données toutes les "resend_all_every_sec""
  "pure_data": {
    "ip": "255.255.255.255",            => "ip": "127.0.0.1", // Par defaut laisser à 127.0.0.1 pour dire que la communication est local
    "port": 8012,
    "resend_all_every_sec": 10,
    "debut_print_osc": true             => A false, ne print plus l'envoie des commandes
  },

  // communication port com arduino pour la partie de Manu
  "main_arduino": {
    "port_com": "/dev/ttyACM0" 
  },
  
  // si jamais la partie de Manu ne fonctionne plus, alors on peut recuperer les valeurs de rotation via mon software
  // Il suffit de brancher X arduino en USB, et d'indiquer les ports coms, l'ordre recu pour enregistrer les valeurs corresponds à 
  // RPM;valeur_rpm_1;...;valeur_rpm_n
  "hall_sensor_arduino": {
    "port_com": "/dev/X /dev/Y",        => liste de port com en format string separé par un espace
    "baudrate": 9600
  },

  // Api pour recevoir via réseau les données envoyés par le bms
  "bms_api": {
    "ip": "0.0.0.0",                    => laisser à 0.0.0.0 pour accepter les packets UDP de toutes les IPs
    "port": 8888,
    "debut_print_osc": true,
    "power_input_on_230V_watt": 500     => la puissance à partir de laquelle on considère que le BMS est branché au secteur
  },

  // Api pour recevoir les données d'info-climat.org
  "info_climat_api": {
    "authentification": "ABpXQFMtACICL1ZhBHIGL1gwUmcJfwEmA39VNlwyBHkHZlU4DmsHbFY8VClVegAxWHUPbF1qCDFRMlc3AHIFeQBhVzpTOQBnAmlWMgQ9Bi1YdFIvCTcBJgN%2FVTpcOQR5B2VVNg5qB3tWMVQ0VXsANlhpD25dfQgvUTNXNwBqBWIAYVc7UzMAagJlVjMEKwYtWG1SOwllATgDZlVhXDcEMQdlVWAOaQc3VjlUN1V7ADBYbg9tXWUIMFE6VzQAaAV5AHxXSlNDAH8CLVZ2BGEGdFh2UmcJaAFt",
    "key": "5843c5d3fb19aea4ac430f5585a78c6d",
    "refresh_rate_sec": 3600            => rafraichissement des requetes après une requête reussi, sinon renvoie la requête constamment jusqu'au succès
  },

  // Api n'utilisant pas internet, mais initialement mis au cas où un API internet serait implémenté
  "ephemeride_api": {
    "refresh_rate_sec": 3600
  },

  
  // Paramètres de calcul pour "/luminosity" 
  // https://www.dcode.fr/points-plotter
  // Fonction : 
  // SI batterie sur secteur renvoie :
  //      return "luminosity_on_230V"
  // SINON renvoie : 
  //      reduction = (1.0 - current_cloud_reducer / 100.0) * (1.0 - battery_remaining_reducer / 100.0) * (1.0 - next_day_sunlight_hour_reducer / 100.0)
  //      return power_to_luminosity * reduction
  // Avec une regression linéaire pour récupérer chacune des valeurs
  "luminosite_scale": {
    "luminosity_on_230V": 100,                                  => luminosite forcé quand on la batterie est branché au secteur
    "power_to_luminosity": "(0,0) (200,50) (300, 100)",         => (power_input, pourcent_luminosite) ->                regression linéaire de la valeur de la puissance en watt transformé % de luminosité. 
    "current_cloud_reducer": "(0,0) (50,10) (100,30)",          => (pourcent_nebulosite_3h, pourcent_luminosite) ->     regression linéaire de la valeur de la nebulosité sur 3h en % transformé en % de reduction de luminosité. 
    "battery_remaining_reducer": "(0,0) (50,10) (100,30)",      => (pourcent_batterie_restant, pourcent_luminosite) ->  regression linéaire de la valeur du pourcentage de batterie restant en % transformé en % de reduction de luminosité. 
    "next_day_sunlight_hour_reducer": "(0,20) (8,0) (24,0)"     => (heure_de_soleil_lendemain, pourcent_luminosite) ->  regression linéaire de la valeur du nombre d'heure de soleil entre le coucher et lever * totale de nebulosité % transformé en % de reduction de luminosité. 
  },
  
  // Paramètres de calcul pour "/duty_cycle" 
  //        duty_cycle_high = 1000.0 * duty_cycle_duration_s * duty_cycle_fill_percent / 100.0
  //        duty_cycle = 1000.0 * duty_cycle_duration_s * (1.0 - duty_cycle_fill_percent / 100.0)
  // Fonction : 
  // durée des états haut et bas du cycle d'allumage extinction de la led
  // Fonction: 
  //        ratio_reduction = (1.0 - current_cloud_reducer / 100.0) * (1.0 - battery_remaining_reducer / 100.0) * (1.0 - next_day_sunlight_hour_reducer / 100.0)
  //        return duty_cycle_duration_s * (2.0 - ratio_reduction) -> permet de recevoir une valeur de 1 fois la durée du duty cycle à 2 fois la durée 
  // Avec une regression linéaire pour récupérer chacune des valeurs
  "duty_cycle_duration_s": {
    "power_to_duration_s": "(0,10.0) (150,4.0) (300,0.6)",      => (durée_duty_cycle, pourcent_luminosite) ->           regression linéaire du durée du cycle d'allumage extinction en sec transformé en % de luminosité. 
    "current_cloud_reducer": "(0,0) (50,10) (100,30)",          => (pourcent_nebulosite_3h, pourcent_luminosite) ->     regression linéaire de la valeur de la nebulosité sur 3h en % transformé en % de reduction de luminosité. 
    "battery_remaining_reducer": "(0,0) (50,10) (100,30)",      => (pourcent_batterie_restant, pourcent_luminosite) ->  regression linéaire de la valeur du pourcentage de batterie restant en % transformé en % de reduction de luminosité. 
    "next_day_sunlight_hour_reducer": "(0,20) (8,0) (24,0)"     => (heure_de_soleil_lendemain, pourcent_luminosite) ->  regression linéaire de la valeur du nombre d'heure de soleil entre le coucher et lever * totale de nebulosité % transformé en % de reduction de luminosité. 
  },
  "duty_cycle_fill_percent": {
    "power_to_fill_percent": "(0,0.0) (200,30.0) (300,60)",     => (pourcentage_etat_haut, pourcent_luminosite) ->      regression linéaire du pourcentage de temps à l'état haut dans le cycle d'allumage extinction en pourcent transformé en % de luminosité. 
    "current_cloud_reducer": "(0,0) (50,10) (100,30)",          => (pourcent_nebulosite_3h, pourcent_luminosite) ->     regression linéaire de la valeur de la nebulosité sur 3h en % transformé en % de reduction luminosité. 
    "battery_remaining_reducer": "(0,0) (50,10) (100,30)",      => (pourcent_batterie_restant, pourcent_luminosite) ->  regression linéaire de la valeur du pourcentage de batterie restant en % transformé en % de de reduction luminosité. 
    "next_day_sunlight_hour_reducer": "(0,20) (8,0) (24,0)"     => (heure_de_soleil_lendemain, pourcent_luminosite) ->  regression linéaire de la valeur du nombre d'heure de soleil entre le coucher et lever * totale de nebulosité % transformé en % de de reduction luminosité. 
  },

  // Paramètre "/day" envoyé 
  // Exemple d'index envoyé :
  // day          [0 , 40 , 60 , 94, 100]
  // night                            [0, 100]
  // index recu      1    2    3    4   5
  "day_slice": {
    "day": "[0, 40, 60, 94, 100]",
    "night": "[0, 100]"
    "force_envoie_day" : -1                             => <= 0 renvoie la valeur normal, superieur >= 1 force "/day" à la valeur écrite
  },

  // fonction de pour relire une météo à partir d'une certaine date plutot que de prendre les valeurs actuelles
  "playback": {
    "activate": false,
    "use_plugged_battery_data": false,                  => A true utilise les données actuelle de la batterie connecté
    "starting_date_iso_format": "2026-07-27T16:02",
    "playback_speed": 60.0
  }
}

