# Configuration

Contient le format du fichier de configuration de l'installation ainsi qu'un script de validation. Usage du script:

## usage

```bash
#important: doit être lancé depuis le répertoire racine du repo, i.e, le répertoire parent.
python3 -m config.check-config
```
